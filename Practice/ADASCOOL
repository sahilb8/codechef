#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    while(n--)
    {
       int m,n;
       scanf("%d %d",&m,&n);
       if((m*n)%2==0)
       printf("YES\n");
       else
        printf("NO\n");

    }
    return 0;

}

